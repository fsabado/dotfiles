#!/bin/sh
#Run all the bash configs with *.bash extension
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null && pwd )"
for i in $DIR/configs/*.bash; do
	[ -f "$i" ] || break
	#echo "$i"
	. "$i"
done
